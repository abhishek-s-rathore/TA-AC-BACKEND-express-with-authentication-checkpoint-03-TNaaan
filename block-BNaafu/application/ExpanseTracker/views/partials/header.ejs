<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!--Title-->
    <title>Expense-Tracker</title>
    <!--Stylesheet-->
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <!--Google Fonts-->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Nunito:ital,wght@0,200;0,300;0,400;0,600;0,700;0,800;0,900;1,200;1,300;1,400;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet"
    />
    <!--Font Awesome-->
    <script
      src="https://kit.fontawesome.com/5cc6fb9f6b.js"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <!--Header-->
    <header class="header">
      <div class="container flex justify-between align-center">
        <a class="logo" href="/"
          ><img
            src="/images/expanse-tracker-logo.png"
            height="70px"
            width="280px"
            alt="Expanse Logo"
        /></a>
        <nav>
          <ul class="flex justify-between align-center">
            <% if(user){ %>
            <li>
              <% if(user.providers.includes('local')){ %>
              <a class="user" href=""> <%= user.localUser.name %></a>
              <% }else if(user.providers.includes('google')){ %>
              <a class="user" href=""> <%= user.googleUser.name %></a>
              <% } else if (user.providers.includes('github')){ %>
              <a class="user" href=""> <%= user.githubUser.name %></a>
              <% } %>
            </li>
            <li>
              <a href="/users/signout"
                ><button class="btn btn-primary">Sign Out</button></a
              >
            </li>
            <% }else{ %>
            <li>
              <a href="/users/signin"
                ><button class="btn btn-primary">Sign In</button></a
              >
            </li>
            <li>
              <a href="/users/signup"
                ><button class="btn btn-primary">Sign Up</button></a
              >
            </li>
            <% } %>
          </ul>
        </nav>
      </div>
    </header>
  </body>
</html>
